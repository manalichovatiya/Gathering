#include<stdio.h>
#include<string.h>
int length(char a[1000]){
	printf("\nLength of the String is : %d", length);
   return 0;
}

int main(){
    char a[1000];
	printf("Enter text : ");
	gets(a);
	int length;
    length = strlen(a);
 	printf("\nLength of the String is : %d", length);

	return 0;
}
